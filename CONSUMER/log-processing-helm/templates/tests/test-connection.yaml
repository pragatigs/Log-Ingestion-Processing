apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "log-processing.fullname" . }}-test-connection"
  labels:
    {{- include "log-processing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "log-processing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
